# Claude Insights Toolkit

Claude Code 사용 데이터를 분석하여 개인 및 팀의 생산성을 향상시키는 도구

## 주요 기능

### 1. API 에러 분석
- API 에러 패턴 및 상관관계 분석
- 시간대별 에러 트렌드
- 에러 유형별 영향도 분석

### 2. 카테고리별 성공률 분석
- 목표 카테고리별 성공률 통계
- 카테고리 간 비교 분석
- 시간에 따른 성공률 트렌드

### 3. 세션 효율성 분석
- 반복 횟수(Iterative Refinement) 분석
- 비효율적 세션 패턴 감지
- 세션당 성공률 및 지표

### 4. 도움 정도 상관관계 분석
- Claude 도움 정도와 결과의 상관관계
- 도움 정도별 성공률 통계
- 사용자 만족도 패턴

### 5. 시간대별 패턴 분석
- 시간대별 활동 패턴
- 최적의 작업 시간대 분석
- 생체리듬 기반 생산성 인사이트

## Insights 활용 가이드

### 주간 리뷰 체크리스트

- [ ] **세션당 평균 비용 추이 확인** (목표: $8 이하)
- [ ] **제안 수락률 분석** (목표: 15% 이상)
- [ ] **캐시 히트 비율 확인** (목표: 10:1 이상)
- [ ] **작업 복잡도 점수 추이 확인**

### 생산성 최적화 패턴

| 상황 | 권장 접근법 |
|------|-------------|
| **Research Day** | 복잡한 분석, 아키텍처 설계 |
| **Implementation Day** | 일상 코딩, 리팩토링 |
| **컨텍스트 리셋** | 관련 없는 작업 전 `/clear` 실행 |
| **서브에이전트 활용** | 조사/검증은 별도 에이전트에 위임 |

### 5대 분석 기능 인사이트

#### 1. API 에러 분석
- 에러 패턴은 시스템의 "건강 상태"를 보여주는 바로미터
- 특정 시간대/엔드포인트 집중 에러는 인프라/설계 문제의 신호
- **권장 액션:** 주간 상위 3개 에러 유형 근본 원인 분석

#### 2. 카테고리별 성공률 분석
- 강점/약점의 가시화 → 전문성 영역 확인
- 리소스 할당의 과학적 근거 제공
- **권장 액션:** 각 카테고리별 목표 성공률 설정 및 모니터링

#### 3. 세션 효율성 분석
- 반복 횟수는 명확성의 척도
- 효율성은 결과의 질과 직결
- **권장 액션:** 자주 수행하는 작업의 프롬프트 템플릿화

#### 4. 도움 정도 상관관계 분석
- 도움 정도와 결과는 양의 상관관계
- **권장 액션:** 세션 종료 시 도움 정도 평가 습관 형성

#### 5. 시간대별 패턴 분석
- 생체리듬(크로노타입)과 인지 능력의 연관성
- **권장 액션:** 중요/복잡한 작업을 개인 최적 시간대에 배치

## 설치 및 설정

### 빠른 시작

```bash
# 1. 설치
npm install

# 2. 자동 수집 설정 (권장)
npm run build
npx cit setup

# 3. 기존 데이터 수집
npx cit collect --all

# 4. 상태 확인
npx cit health
```

### 자동 수집 방법

**옵션 1: 훅 기반 (권장)**
- `cit setup` 명령으로 UserPromptSubmit 훅 자동 설정
- Claude Code 세션 후 자동으로 데이터 수집
- 설정 마찰 최소화

**옵션 2: 데몬 기반 (선택)**
- `cit daemon start`로 파일 감시 데몬 시작
- 실시간 데이터 수집 (light 모드)
- 백그라운드 실행

## 데이터 진단

### 무결성 검사

```bash
# 데이터 무결성 전체 검사
npx cit doctor
```

**검사 항목:**
- 데이터 디렉토리 존재 여부
- 파일 수 및 유효성
- JSON 파일 무결성
- 세션 중복률 분석
- 날짜 연속성 (gap 탐지)
- 스토리지 사용량

### 설정 진단

```bash
# 자동 수집 상태 확인
npx cit health

# 데몬 상태 확인
npx cit daemon status
```

**검사 항목:**
- Source/output 경로
- 자동 수집 훅 설정
- 데이터 최신성

## 개발

```bash
# 개발 서버
npm run dev

# 빌드
npm run build
```

## 라이선스

MIT
