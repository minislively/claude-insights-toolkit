// Re-export types from shared CLI project (type-only, no runtime dependency)
export type {
  IInsightsDay,
  ISessionFacet,
  ICountObject,
} from '@shared/types/insights'

export {
  Outcome,
  ClaudeHelpfulness,
  SessionType,
  PrimarySuccess,
} from '@shared/types/insights'
